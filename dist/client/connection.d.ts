import { EventEmitter } from 'events';
import { NethernetClient } from '../nethernet';
import { RaknetClient } from '../rak';
import Framer from '../transforms/framer';
import { CompressionAlgorithm } from '../types';
export declare class Connection extends EventEmitter {
    #private;
    connection: RaknetClient | NethernetClient;
    encryptionEnabled: boolean;
    disableEncryption: boolean;
    compressionReady: boolean;
    compressionAlgorithm: CompressionAlgorithm;
    compressionThreshold: number;
    compressionHeader: number;
    compressionLevel: number;
    batchHeader: number | null;
    framer: Framer;
    decrypt: any;
    encrypt: any;
    sendQ: Buffer[];
    loop: NodeJS.Timeout;
    serializer: any;
    deserializer: any;
    constructor();
    get status(): number;
    set status(val: number);
    startEncryption(iv: any): void;
    write(name: any, params: any): void;
    queue(name: any, params: any): void;
    _tick(): void;
    onTick: () => void;
    sendBuffer(buffer: Buffer, immediate?: boolean): number | undefined;
    sendEncryptedBatch(batch: Framer): void;
    sendDecryptedBatch(batch: Framer): void;
    sendPackets(buffer: Buffer, immediate: boolean): void;
    onEncryptedPacket: (buf: Buffer) => void;
    onDecryptedPacket: (buf: Buffer) => void;
    handle(buffer: Buffer): void;
}
