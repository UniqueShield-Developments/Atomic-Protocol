{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "outputs": [
        "dist/**"
      ]
    },
    "bundle": {
      "dependsOn": [
        "build"
      ],
      "outputs": [
        "bundle/**"
      ]
    },
    "lint": {
      "outputs": []
    }
  }
}